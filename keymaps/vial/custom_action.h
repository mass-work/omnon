#ifndef CUSTOM_ACTION_H
#define CUSTOM_ACTION_H

#include <stdint.h> // uint8_t の定義
#include <stdbool.h> // bool の定義

void custom_register_mouse(uint8_t mouse_keycode, bool pressed);

#endif // CUSTOM_ACTION_H
